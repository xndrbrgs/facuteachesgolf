const SchoolSection = () => {
  return (
    <section className="relative bg-grayback px-[clamp(16px,40px)] text-black w-full">
      <h1 className="h1">GOLF SCHOOLS</h1>
    </section>
  );
};

export default SchoolSection;
